import { CalendarComponent } from "@syncfusion/ej2-react-calendars";

function Calendars({ width, height }) {
  return <CalendarComponent id="calendar" style={{ width, height }} />;
}

export default Calendars;
